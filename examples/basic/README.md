# Basic example

A minimal Playwright Checkpoint project against [playwright.dev](https://playwright.dev).

## Run it

```bash
npm install
npx playwright install
npx playwright test
```

The example includes:
- a basic checkpoint
- a documentation-focused checkpoint with `description` and `step`
- a checkpoint with collector overrides and a highlighted element
